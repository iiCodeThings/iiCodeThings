# 个人学术对话系统 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Ubuntu 私有云上交付一套单用户、可切换 OpenAI 兼容模型、会话可持久化（含 reasoning 与模型快照）的 Vue 3 + FastAPI 对话应用。

**Architecture:** FastAPI 提供 Cookie 鉴权、CRUD、附件处理，并把上游 `chat.completions` 流转成 SSE；Vue 3 SPA 负责左栏导航/输入与右栏无限滚动聊天。数据在本机 MySQL `chat_db`，文件在本地上传目录。Supervisor 只拉起 uvicorn，Nginx 托管前端并反代 `/api`。

**Tech Stack:** Python 3.11、FastAPI、SQLAlchemy 2、PyMySQL、httpx、python-docx、Vue 3、Vite、vue-router、Nginx、Supervisor。

## Global Constraints

- 单用户：用户名 + 密码；无注册、无多用户隔离。
- 只对接 OpenAI 兼容 `chat.completions`（`stream: true`）。
- MySQL：`127.0.0.1`、用户 `root`、空密码、库 `chat_db`。
- 启动必填环境变量：`APP_USERNAME`、`APP_PASSWORD`、`SESSION_SECRET`；缺一拒绝启动。
- 每条 `messages` 写入 `model_name` 与 `model` 快照；不把历史 `reasoning` 回传给模型。
- 附件：图片 `.jpg` `.jpeg` `.png` `.webp`；文档 `.txt` `.md` `.doc` `.docx`；单文件上限 20MB。
- `.docx` 用 `python-docx`；`.doc` 用系统命令 `antiword`。
- 搜索：空格分词 AND；只搜 session 标题与 `messages.content`。
- 登录失败文案固定为「用户名或密码错误」。
- 第一版不做：RAG、学科人设、消息编辑/分支、Docker、把 reasoning 送回模型。
- 进程：Supervisor 管理 uvicorn；Nginx 用系统服务。
- 规范：`FOR_LLM.md` 与 `docs/superpowers/specs/2026-09-04-ai-chat-design.md`。

---

## File Structure

```
backend/requirements.txt
backend/app/config.py          # Settings、load_settings()
backend/app/db.py              # engine、SessionLocal、get_db、init_db
backend/app/tables.py          # User, LlmModel, ChatSession, Message, Attachment
backend/app/security.py        # 密码哈希、Cookie 签发/校验
backend/app/deps.py            # get_current_user
backend/app/main.py            # create_app()
backend/app/title.py           # auto_title()
backend/app/routers/auth.py
backend/app/routers/llm_models.py
backend/app/routers/sessions.py
backend/app/routers/messages.py
backend/app/routers/search.py
backend/app/services/attachments.py
backend/app/services/context.py
backend/app/services/llm.py
backend/tests/conftest.py
backend/tests/test_*.py
frontend/package.json
frontend/vite.config.js
frontend/index.html
frontend/src/main.js
frontend/src/api.js
frontend/src/App.vue
frontend/src/views/LoginView.vue
frontend/src/views/ChatView.vue
frontend/src/views/SettingsView.vue
frontend/src/components/AppShell.vue
frontend/src/components/Composer.vue
frontend/src/components/MessagePane.vue
deploy/chat-api.conf            # Supervisor
deploy/nginx-chat.conf
.env.example
.gitignore
```

表名 `sessions` 对应类名 `ChatSession`，避免与 SQLAlchemy `Session` 重名。Cookie 名：`chat_session`。上传目录配置项：`UPLOAD_DIR`，默认 `/var/lib/iicode-chat/uploads`。

---

### Task 1: 配置与应用工厂

**Files:**
- Create: `backend/requirements.txt`
- Create: `backend/app/__init__.py`
- Create: `backend/app/config.py`
- Create: `backend/app/main.py`
- Create: `backend/tests/test_config.py`
- Create: `.gitignore`

**Interfaces:**
- Consumes: 环境变量 `APP_USERNAME`、`APP_PASSWORD`、`SESSION_SECRET`、可选 MySQL/上传配置。
- Produces: `class Settings`；`def load_settings() -> Settings`（缺必填项 raise `SystemExit`）；`def create_app(settings: Settings | None = None) -> FastAPI`。

- [ ] **Step 1: Write the failing test**

Create `backend/tests/test_config.py`:

```python
import pytest
from pydantic import ValidationError

from app.config import Settings, load_settings


def test_settings_accepts_required_fields():
    s = Settings(
        app_username="admin",
        app_password="pass",
        session_secret="secret-secret-secret-secret",
    )
    assert s.mysql_host == "127.0.0.1"
    assert s.mysql_user == "root"
    assert s.mysql_password == ""
    assert s.mysql_database == "chat_db"
    assert s.max_upload_bytes == 20 * 1024 * 1024


def test_load_settings_exits_when_secret_missing(monkeypatch):
    monkeypatch.delenv("APP_USERNAME", raising=False)
    monkeypatch.delenv("APP_PASSWORD", raising=False)
    monkeypatch.delenv("SESSION_SECRET", raising=False)
    with pytest.raises(SystemExit):
        load_settings()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && pip install pydantic-settings pytest && PYTHONPATH=. pytest tests/test_config.py -v`

Expected: FAIL with `ModuleNotFoundError: No module named 'app.config'`

- [ ] **Step 3: Write minimal implementation**

`backend/requirements.txt`:

```
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pymysql==1.1.1
pydantic-settings==2.7.0
python-multipart==0.0.20
httpx==0.28.1
python-docx==1.1.2
itsdangerous==2.2.0
pytest==8.3.4
pytest-asyncio==0.25.2
```

`backend/app/__init__.py`: empty.

`backend/app/config.py`:

```python
from pydantic import Field
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", extra="ignore")

    app_username: str = Field(validation_alias="APP_USERNAME")
    app_password: str = Field(validation_alias="APP_PASSWORD")
    session_secret: str = Field(validation_alias="SESSION_SECRET")
    mysql_host: str = Field(default="127.0.0.1", validation_alias="MYSQL_HOST")
    mysql_user: str = Field(default="root", validation_alias="MYSQL_USER")
    mysql_password: str = Field(default="", validation_alias="MYSQL_PASSWORD")
    mysql_database: str = Field(default="chat_db", validation_alias="MYSQL_DATABASE")
    database_url: str | None = Field(default=None, validation_alias="DATABASE_URL")
    upload_dir: str = Field(
        default="/var/lib/iicode-chat/uploads", validation_alias="UPLOAD_DIR"
    )
    max_upload_bytes: int = Field(
        default=20 * 1024 * 1024, validation_alias="MAX_UPLOAD_BYTES"
    )
    cookie_name: str = "chat_session"
    cookie_max_age: int = 60 * 60 * 24 * 30


def load_settings() -> Settings:
    try:
        return Settings()
    except Exception as exc:
        raise SystemExit(f"missing required settings: {exc}") from exc
```

`backend/app/main.py`:

```python
from fastapi import FastAPI

from app.config import Settings, load_settings


def create_app(settings: Settings | None = None) -> FastAPI:
    settings = settings or load_settings()
    app = FastAPI(title="iiCode Chat")
    app.state.settings = settings
    return app
```

`.gitignore`:

```
.env
__pycache__/
.venv/
venv/
backend/.venv/
frontend/node_modules/
frontend/dist/
*.pyc
.pytest_cache/
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && pip install -r requirements.txt && PYTHONPATH=. pytest tests/test_config.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add .gitignore backend/requirements.txt backend/app/__init__.py backend/app/config.py backend/app/main.py backend/tests/test_config.py
git commit -m "feat: add FastAPI app factory and required settings"
```

---

### Task 2: 数据表与建表种子

**Files:**
- Create: `backend/app/tables.py`
- Create: `backend/app/db.py`
- Create: `backend/tests/conftest.py`
- Create: `backend/tests/test_tables.py`
- Modify: `backend/app/main.py`

**Interfaces:**
- Consumes: `Settings.database_url` 或由 MySQL 字段拼出的 URL。
- Produces: ORM 类 `User`、`LlmModel`、`ChatSession`、`Message`、`Attachment`；`def get_engine(settings: Settings)`；`def init_db(engine) -> None`；`def seed_default_user(db, username: str, password_hash: str) -> None`；`def get_db()` 生成器。

- [ ] **Step 1: Write the failing test**

Create `backend/tests/test_tables.py`:

```python
from app.security import hash_password
from app.tables import User


def test_seed_creates_single_user(db, settings):
    from app.db import seed_default_user

    seed_default_user(db, settings.app_username, hash_password(settings.app_password))
    seed_default_user(db, settings.app_username, hash_password("other"))
    users = db.query(User).all()
    assert len(users) == 1
    assert users[0].username == "admin"
    assert users[0].token_version == 0
```

Create `backend/tests/conftest.py`:

```python
import pytest
from fastapi.testclient import TestClient
from sqlalchemy.orm import sessionmaker

from app.config import Settings
from app.db import get_db, get_engine, init_db
from app.main import create_app


@pytest.fixture
def settings(tmp_path):
    return Settings(
        app_username="admin",
        app_password="passpass",
        session_secret="secret-secret-secret-secret",
        database_url=f"sqlite:///{tmp_path}/test.db",
        upload_dir=str(tmp_path / "uploads"),
    )


@pytest.fixture
def engine(settings):
    engine = get_engine(settings)
    init_db(engine)
    return engine


@pytest.fixture
def db(engine):
    SessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)
    session = SessionLocal()
    try:
        yield session
    finally:
        session.close()


@pytest.fixture
def client(settings, engine):
    SessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)
    app = create_app(settings)

    def _get_db():
        session = SessionLocal()
        try:
            yield session
        finally:
            session.close()

    app.dependency_overrides[get_db] = _get_db
    with TestClient(app) as c:
        yield c
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && PYTHONPATH=. pytest tests/test_tables.py -v`

Expected: FAIL with `ModuleNotFoundError: No module named 'app.db'` or `app.security`

- [ ] **Step 3: Write minimal implementation**

先放一个临时 `backend/app/security.py`（Task 3 会换成完整实现；此处仅需 `hash_password` 让种子测试能跑）。若 Step 2 因 `security` 失败，先写：

```python
import hashlib
import os


def hash_password(password: str) -> str:
    salt = os.urandom(16)
    dk = hashlib.pbkdf2_hmac("sha256", password.encode("utf-8"), salt, 100_000)
    return f"{salt.hex()}${dk.hex()}"
```

`backend/app/tables.py`:

```python
from datetime import datetime

from sqlalchemy import Boolean, DateTime, ForeignKey, Integer, String, Text, func
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column, relationship


class Base(DeclarativeBase):
    pass


class User(Base):
    __tablename__ = "users"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    username: Mapped[str] = mapped_column(String(64), unique=True, nullable=False)
    password_hash: Mapped[str] = mapped_column(String(255), nullable=False)
    token_version: Mapped[int] = mapped_column(Integer, nullable=False, default=0)
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(
        DateTime, server_default=func.now(), onupdate=func.now()
    )


class LlmModel(Base):
    __tablename__ = "llm_models"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    name: Mapped[str] = mapped_column(String(128), nullable=False)
    base_url: Mapped[str] = mapped_column(String(512), nullable=False)
    api_key: Mapped[str] = mapped_column(String(512), nullable=False)
    model: Mapped[str] = mapped_column(String(128), nullable=False)
    supports_vision: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)
    sort_order: Mapped[int] = mapped_column(Integer, nullable=False, default=0)
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(
        DateTime, server_default=func.now(), onupdate=func.now()
    )


class ChatSession(Base):
    __tablename__ = "sessions"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    title: Mapped[str] = mapped_column(String(128), nullable=False, default="新对话")
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(
        DateTime, server_default=func.now(), onupdate=func.now()
    )
    messages: Mapped[list["Message"]] = relationship(
        back_populates="session", cascade="all, delete-orphan"
    )


class Message(Base):
    __tablename__ = "messages"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    session_id: Mapped[int] = mapped_column(
        ForeignKey("sessions.id", ondelete="CASCADE"), nullable=False, index=True
    )
    role: Mapped[str] = mapped_column(String(16), nullable=False)
    content: Mapped[str] = mapped_column(Text, nullable=False, default="")
    reasoning: Mapped[str | None] = mapped_column(Text, nullable=True)
    model_name: Mapped[str] = mapped_column(String(128), nullable=False)
    model: Mapped[str] = mapped_column(String(128), nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
    session: Mapped["ChatSession"] = relationship(back_populates="messages")
    attachments: Mapped[list["Attachment"]] = relationship(
        back_populates="message", cascade="all, delete-orphan"
    )


class Attachment(Base):
    __tablename__ = "attachments"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    message_id: Mapped[int] = mapped_column(
        ForeignKey("messages.id", ondelete="CASCADE"), nullable=False
    )
    kind: Mapped[str] = mapped_column(String(16), nullable=False)
    original_filename: Mapped[str] = mapped_column(String(255), nullable=False)
    mime_type: Mapped[str] = mapped_column(String(128), nullable=False)
    storage_path: Mapped[str] = mapped_column(String(512), nullable=False)
    extracted_text: Mapped[str | None] = mapped_column(Text, nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
    message: Mapped["Message"] = relationship(back_populates="attachments")
```

`backend/app/db.py`:

```python
from collections.abc import Generator

from sqlalchemy import create_engine
from sqlalchemy.engine import Engine
from sqlalchemy.orm import Session, sessionmaker

from app.config import Settings
from app.tables import Base, User


def mysql_url(settings: Settings) -> str:
    password = settings.mysql_password
    auth = settings.mysql_user if password == "" else f"{settings.mysql_user}:{password}"
    return f"mysql+pymysql://{auth}@{settings.mysql_host}/{settings.mysql_database}?charset=utf8mb4"


def get_engine(settings: Settings) -> Engine:
    url = settings.database_url or mysql_url(settings)
    kwargs = {"future": True, "pool_pre_ping": True}
    if url.startswith("sqlite"):
        kwargs["connect_args"] = {"check_same_thread": False}
    return create_engine(url, **kwargs)


SessionLocal: sessionmaker[Session] | None = None


def init_session_factory(engine: Engine) -> sessionmaker[Session]:
    global SessionLocal
    SessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)
    return SessionLocal


def init_db(engine: Engine) -> None:
    Base.metadata.create_all(engine)


def seed_default_user(db: Session, username: str, password_hash: str) -> None:
    if db.query(User).count() == 0:
        db.add(User(username=username, password_hash=password_hash, token_version=0))
        db.commit()


def get_db() -> Generator[Session, None, None]:
    if SessionLocal is None:
        raise RuntimeError("SessionLocal not initialized")
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

Modify `create_app` in `backend/app/main.py` to init engine, tables, and seed on startup:

```python
from pathlib import Path

from fastapi import FastAPI

from app.config import Settings, load_settings
from app.db import get_engine, init_db, init_session_factory, seed_default_user
from app.security import hash_password


def create_app(settings: Settings | None = None) -> FastAPI:
    settings = settings or load_settings()
    Path(settings.upload_dir).mkdir(parents=True, exist_ok=True)
    engine = get_engine(settings)
    init_db(engine)
    init_session_factory(engine)

    app = FastAPI(title="iiCode Chat")
    app.state.settings = settings
    app.state.engine = engine

    from app.db import SessionLocal

    db = SessionLocal()
    try:
        seed_default_user(
            db, settings.app_username, hash_password(settings.app_password)
        )
    finally:
        db.close()
    return app
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && PYTHONPATH=. pytest tests/test_tables.py tests/test_config.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/tables.py backend/app/db.py backend/app/main.py backend/app/security.py backend/tests/conftest.py backend/tests/test_tables.py
git commit -m "feat: add MySQL schema, sqlite test db, and default user seed"
```

---

### Task 3: 密码哈希与登录 Cookie

**Files:**
- Modify: `backend/app/security.py`
- Create: `backend/tests/test_security.py`

**Interfaces:**
- Consumes: `Settings.session_secret`、`Settings.cookie_max_age`。
- Produces: `hash_password(password: str) -> str`；`verify_password(password: str, password_hash: str) -> bool`；`sign_session(user_id: int, token_version: int, secret: str) -> str`；`read_session(token: str, secret: str) -> tuple[int, int] | None`。Cookie 载荷字段名为 `user_id`、`token_version`。

- [ ] **Step 1: Write the failing test**

```python
from app.security import hash_password, read_session, sign_session, verify_password


def test_password_roundtrip():
    hashed = hash_password("passpass")
    assert verify_password("passpass", hashed)
    assert not verify_password("wrong", hashed)


def test_session_cookie_roundtrip():
    secret = "secret-secret-secret-secret"
    token = sign_session(1, 3, secret)
    assert read_session(token, secret) == (1, 3)
    assert read_session(token, "other-secret-other-secret") is None
    assert read_session("not-a-token", secret) is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && PYTHONPATH=. pytest tests/test_security.py -v`

Expected: FAIL with `ImportError` or `verify_password` / `sign_session` not defined

- [ ] **Step 3: Write minimal implementation**

Replace `backend/app/security.py` with:

```python
import hashlib
import os

from itsdangerous import BadSignature, SignatureExpired, URLSafeTimedSerializer

MAX_AGE = 60 * 60 * 24 * 30


def hash_password(password: str) -> str:
    salt = os.urandom(16)
    dk = hashlib.pbkdf2_hmac("sha256", password.encode("utf-8"), salt, 100_000)
    return f"{salt.hex()}${dk.hex()}"


def verify_password(password: str, password_hash: str) -> bool:
    try:
        salt_hex, dk_hex = password_hash.split("$", 1)
    except ValueError:
        return False
    salt = bytes.fromhex(salt_hex)
    expected = bytes.fromhex(dk_hex)
    actual = hashlib.pbkdf2_hmac("sha256", password.encode("utf-8"), salt, 100_000)
    return hashlib.compare_digest(actual, expected)


def _serializer(secret: str) -> URLSafeTimedSerializer:
    return URLSafeTimedSerializer(secret, salt="chat-session")


def sign_session(user_id: int, token_version: int, secret: str) -> str:
    return _serializer(secret).dumps({"user_id": user_id, "token_version": token_version})


def read_session(token: str, secret: str) -> tuple[int, int] | None:
    try:
        data = _serializer(secret).loads(token, max_age=MAX_AGE)
        return int(data["user_id"]), int(data["token_version"])
    except (BadSignature, SignatureExpired, KeyError, TypeError, ValueError):
        return None
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && PYTHONPATH=. pytest tests/test_security.py tests/test_tables.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/security.py backend/tests/test_security.py
git commit -m "feat: add password hashing and signed session cookies"
```

---

### Task 4: 登录、登出、改密 API

**Files:**
- Create: `backend/app/deps.py`
- Create: `backend/app/routers/__init__.py`
- Create: `backend/app/routers/auth.py`
- Create: `backend/tests/test_auth_api.py`
- Modify: `backend/app/main.py`（include router）

**Interfaces:**
- Consumes: `hash_password`、`verify_password`、`sign_session`、`read_session`、`User`、`get_db`、`app.state.settings`。
- Produces: `POST /api/auth/login`（body `{"username","password"}`，成功 Set-Cookie `chat_session`）；`POST /api/auth/logout`；`POST /api/auth/password`（body `{"old_password","new_password"}`，成功后 `token_version += 1` 并清 Cookie）；`get_current_user` 依赖，失败 HTTP 401。登录失败 JSON `{"detail": "用户名或密码错误"}`。

- [ ] **Step 1: Write the failing test**

```python
from app.db import seed_default_user
from app.security import hash_password


def test_login_rejects_wrong_password(client, db, settings):
    seed_default_user(db, settings.app_username, hash_password(settings.app_password))
    r = client.post("/api/auth/login", json={"username": "admin", "password": "nope"})
    assert r.status_code == 401
    assert r.json()["detail"] == "用户名或密码错误"


def test_login_logout_and_password_change(client, db, settings):
    seed_default_user(db, settings.app_username, hash_password(settings.app_password))
    r = client.post("/api/auth/login", json={"username": "admin", "password": "passpass"})
    assert r.status_code == 200
    assert client.cookies.get("chat_session")

    r = client.post(
        "/api/auth/password",
        json={"old_password": "passpass", "new_password": "newpass1"},
    )
    assert r.status_code == 200

    r = client.post("/api/auth/login", json={"username": "admin", "password": "passpass"})
    assert r.status_code == 401

    r = client.post("/api/auth/login", json={"username": "admin", "password": "newpass1"})
    assert r.status_code == 200
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && PYTHONPATH=. pytest tests/test_auth_api.py -v`

Expected: FAIL with 404 on `/api/auth/login`

- [ ] **Step 3: Write minimal implementation**

`backend/app/deps.py`:

```python
from fastapi import Depends, HTTPException, Request
from sqlalchemy.orm import Session

from app.db import get_db
from app.security import read_session
from app.tables import User


def get_current_user(request: Request, db: Session = Depends(get_db)) -> User:
    settings = request.app.state.settings
    token = request.cookies.get(settings.cookie_name)
    if not token:
        raise HTTPException(status_code=401, detail="未登录")
    parsed = read_session(token, settings.session_secret)
    if parsed is None:
        raise HTTPException(status_code=401, detail="未登录")
    user_id, token_version = parsed
    user = db.get(User, user_id)
    if user is None or user.token_version != token_version:
        raise HTTPException(status_code=401, detail="未登录")
    return user
```

`backend/app/routers/__init__.py`: empty.

`backend/app/routers/auth.py`:

```python
from fastapi import APIRouter, Depends, HTTPException, Request, Response
from pydantic import BaseModel, Field
from sqlalchemy.orm import Session

from app.db import get_db
from app.deps import get_current_user
from app.security import hash_password, sign_session, verify_password
from app.tables import User

router = APIRouter(prefix="/api/auth", tags=["auth"])


class LoginBody(BaseModel):
    username: str
    password: str


class PasswordBody(BaseModel):
    old_password: str
    new_password: str = Field(min_length=6)


def _set_cookie(response: Response, request: Request, user: User) -> None:
    settings = request.app.state.settings
    token = sign_session(user.id, user.token_version, settings.session_secret)
    response.set_cookie(
        settings.cookie_name,
        token,
        max_age=settings.cookie_max_age,
        httponly=True,
        samesite="lax",
        path="/",
    )


@router.post("/login")
def login(body: LoginBody, request: Request, response: Response, db: Session = Depends(get_db)):
    user = db.query(User).filter(User.username == body.username).one_or_none()
    if user is None or not verify_password(body.password, user.password_hash):
        raise HTTPException(status_code=401, detail="用户名或密码错误")
    _set_cookie(response, request, user)
    return {"ok": True}


@router.post("/logout")
def logout(request: Request, response: Response):
    response.delete_cookie(request.app.state.settings.cookie_name, path="/")
    return {"ok": True}


@router.post("/password")
def change_password(
    body: PasswordBody,
    request: Request,
    response: Response,
    db: Session = Depends(get_db),
    user: User = Depends(get_current_user),
):
    if not verify_password(body.old_password, user.password_hash):
        raise HTTPException(status_code=401, detail="用户名或密码错误")
    user.password_hash = hash_password(body.new_password)
    user.token_version += 1
    db.commit()
    response.delete_cookie(request.app.state.settings.cookie_name, path="/")
    return {"ok": True}
```

In `create_app`, after creating `app`:

```python
from app.routers import auth as auth_router

app.include_router(auth_router.router)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && PYTHONPATH=. pytest tests/test_auth_api.py tests/test_security.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/deps.py backend/app/routers backend/app/main.py backend/tests/test_auth_api.py
git commit -m "feat: add login, logout, and password change APIs"
```

---

### Task 5: 模型 CRUD（掩码 api_key）

**Files:**
- Create: `backend/app/routers/llm_models.py`
- Create: `backend/tests/test_llm_models_api.py`
- Modify: `backend/app/main.py`

**Interfaces:**
- Consumes: `get_current_user`、`LlmModel`。
- Produces: `GET/POST/PATCH/DELETE /api/models`。列表/详情 JSON 字段：`id, name, base_url, api_key_masked, model, supports_vision, sort_order`。`api_key_masked` 为 `'****' + api_key[-4:]`（不足 4 位则全 `*`）。POST body：`name, base_url, api_key, model, supports_vision, sort_order`。PATCH 的 `api_key` 缺省表示不改。

- [ ] **Step 1: Write the failing test**

```python
from app.db import seed_default_user
from app.security import hash_password


def _login(client, db, settings):
    seed_default_user(db, settings.app_username, hash_password(settings.app_password))
    client.post("/api/auth/login", json={"username": "admin", "password": "passpass"})


def test_models_crud_masks_key(client, db, settings):
    _login(client, db, settings)
    r = client.post(
        "/api/models",
        json={
            "name": "DeepSeek",
            "base_url": "https://api.deepseek.com/v1",
            "api_key": "sk-abcdefghij",
            "model": "deepseek-chat",
            "supports_vision": False,
            "sort_order": 1,
        },
    )
    assert r.status_code == 200
    body = r.json()
    assert body["api_key_masked"] == "****ghij"
    assert "api_key" not in body or body.get("api_key") in (None, body["api_key_masked"])

    listed = client.get("/api/models").json()
    assert listed[0]["name"] == "DeepSeek"
    assert listed[0]["api_key_masked"].startswith("****")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && PYTHONPATH=. pytest tests/test_llm_models_api.py -v`

Expected: FAIL with 404

- [ ] **Step 3: Write minimal implementation**

```python
from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel, Field
from sqlalchemy.orm import Session

from app.db import get_db
from app.deps import get_current_user
from app.tables import LlmModel, User

router = APIRouter(prefix="/api/models", tags=["models"])


class ModelIn(BaseModel):
    name: str
    base_url: str
    api_key: str | None = None
    model: str
    supports_vision: bool = False
    sort_order: int = 0


def _mask(key: str) -> str:
    if len(key) <= 4:
        return "****"
    return "****" + key[-4:]


def _out(row: LlmModel) -> dict:
    return {
        "id": row.id,
        "name": row.name,
        "base_url": row.base_url,
        "api_key_masked": _mask(row.api_key),
        "model": row.model,
        "supports_vision": row.supports_vision,
        "sort_order": row.sort_order,
    }


@router.get("")
def list_models(db: Session = Depends(get_db), user: User = Depends(get_current_user)):
    rows = db.query(LlmModel).order_by(LlmModel.sort_order, LlmModel.id).all()
    return [_out(r) for r in rows]


@router.post("")
def create_model(body: ModelIn, db: Session = Depends(get_db), user: User = Depends(get_current_user)):
    if not body.api_key:
        raise HTTPException(status_code=400, detail="api_key 必填")
    row = LlmModel(
        name=body.name,
        base_url=body.base_url.rstrip("/"),
        api_key=body.api_key,
        model=body.model,
        supports_vision=body.supports_vision,
        sort_order=body.sort_order,
    )
    db.add(row)
    db.commit()
    db.refresh(row)
    return _out(row)


@router.patch("/{model_id}")
def update_model(
    model_id: int,
    body: ModelIn,
    db: Session = Depends(get_db),
    user: User = Depends(get_current_user),
):
    row = db.get(LlmModel, model_id)
    if row is None:
        raise HTTPException(status_code=404, detail="模型不存在")
    row.name = body.name
    row.base_url = body.base_url.rstrip("/")
    row.model = body.model
    row.supports_vision = body.supports_vision
    row.sort_order = body.sort_order
    if body.api_key:
        row.api_key = body.api_key
    db.commit()
    db.refresh(row)
    return _out(row)


@router.delete("/{model_id}")
def delete_model(model_id: int, db: Session = Depends(get_db), user: User = Depends(get_current_user)):
    row = db.get(LlmModel, model_id)
    if row is None:
        raise HTTPException(status_code=404, detail="模型不存在")
    db.delete(row)
    db.commit()
    return {"ok": True}
```

`app.include_router` 该 router。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && PYTHONPATH=. pytest tests/test_llm_models_api.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/routers/llm_models.py backend/app/main.py backend/tests/test_llm_models_api.py
git commit -m "feat: add OpenAI-compatible model CRUD with masked keys"
```

---

### Task 6: Session CRUD 与自动标题

**Files:**
- Create: `backend/app/title.py`
- Create: `backend/app/routers/sessions.py`
- Create: `backend/tests/test_title.py`
- Create: `backend/tests/test_sessions_api.py`
- Modify: `backend/app/main.py`

**Interfaces:**
- Consumes: `ChatSession`、`get_current_user`。
- Produces: `auto_title(content: str, first_filename: str | None) -> str`（去换行，最多 40 字符；正文空则用文件名去扩展名）；`GET/POST /api/sessions`；`PATCH /api/sessions/{id}` body `{"title"}`；`DELETE /api/sessions/{id}` 删除行及其附件文件。列表按 `updated_at` 倒序。默认标题「新对话」。

- [ ] **Step 1: Write the failing test**

`backend/tests/test_title.py`:

```python
from app.title import auto_title


def test_auto_title_from_content():
    assert auto_title("hello\nworld " + "x" * 50, None) == ("hello world " + "x" * 50)[:40]


def test_auto_title_from_filename():
    assert auto_title("  ", "notes.md") == "notes"
```

`backend/tests/test_sessions_api.py`：登录后 POST 创建，断言 `title == "新对话"`；PATCH 改名；DELETE 后 GET 不再包含。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && PYTHONPATH=. pytest tests/test_title.py tests/test_sessions_api.py -v`

Expected: FAIL `No module named 'app.title'` / 404

- [ ] **Step 3: Write minimal implementation**

`backend/app/title.py`:

```python
from pathlib import Path


def auto_title(content: str, first_filename: str | None) -> str:
    text = " ".join((content or "").split())
    if text:
        return text[:40]
    if first_filename:
        return Path(first_filename).stem[:40]
    return "新对话"
```

`backend/app/routers/sessions.py`:

```python
from datetime import datetime, timezone
from pathlib import Path

from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel
from sqlalchemy.orm import Session

from app.db import get_db
from app.deps import get_current_user
from app.tables import Attachment, ChatSession, Message, User

router = APIRouter(prefix="/api/sessions", tags=["sessions"])


def _out(row: ChatSession) -> dict:
    return {
        "id": row.id,
        "title": row.title,
        "created_at": row.created_at,
        "updated_at": row.updated_at,
    }


def delete_session_files(db: Session, session_id: int) -> None:
    paths = (
        db.query(Attachment.storage_path)
        .join(Message, Attachment.message_id == Message.id)
        .filter(Message.session_id == session_id)
        .all()
    )
    for (p,) in paths:
        Path(p).unlink(missing_ok=True)


@router.get("")
def list_sessions(db: Session = Depends(get_db), user: User = Depends(get_current_user)):
    rows = db.query(ChatSession).order_by(ChatSession.updated_at.desc()).all()
    return [_out(r) for r in rows]


@router.post("")
def create_session(db: Session = Depends(get_db), user: User = Depends(get_current_user)):
    row = ChatSession(title="新对话")
    db.add(row)
    db.commit()
    db.refresh(row)
    return _out(row)


class TitleIn(BaseModel):
    title: str


@router.patch("/{session_id}")
def patch_session(
    session_id: int,
    body: TitleIn,
    db: Session = Depends(get_db),
    user: User = Depends(get_current_user),
):
    row = db.get(ChatSession, session_id)
    if row is None:
        raise HTTPException(status_code=404, detail="会话不存在")
    row.title = body.title[:128]
    db.commit()
    db.refresh(row)
    return _out(row)


@router.delete("/{session_id}")
def delete_session(
    session_id: int, db: Session = Depends(get_db), user: User = Depends(get_current_user)
):
    row = db.get(ChatSession, session_id)
    if row is None:
        raise HTTPException(status_code=404, detail="会话不存在")
    delete_session_files(db, session_id)
    db.delete(row)
    db.commit()
    return {"ok": True}
```

`app.include_router` 该 router。 DELETE 先删磁盘文件再删行（ORM cascade 删除 messages/attachments）。GET 返回 `[{id, title, created_at, updated_at}]`。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && PYTHONPATH=. pytest tests/test_title.py tests/test_sessions_api.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/title.py backend/app/routers/sessions.py backend/app/main.py backend/tests/test_title.py backend/tests/test_sessions_api.py
git commit -m "feat: add chat sessions CRUD and auto-title helper"
```

---

### Task 7: 附件校验、存盘、抽文本

**Files:**
- Create: `backend/app/services/__init__.py`
- Create: `backend/app/services/attachments.py`
- Create: `backend/tests/test_attachments.py`

**Interfaces:**
- Consumes: `Settings.upload_dir`、`Settings.max_upload_bytes`。
- Produces: `class UploadRejected(Exception)` with `status_code: int`、`detail: str`；`def classify(filename: str) -> str` 返回 `"image"` 或 `"document"`，否则 raise；`def save_bytes(upload_dir: str, message_id: int, filename: str, data: bytes, max_bytes: int) -> str` 返回绝对路径；`def extract_text(path: str, suffix: str) -> str | None`（`.txt`/`.md` 读 utf-8；`.docx` 用 python-docx 拼 paragraph；`.doc` 调 `antiword`，失败返回 `None`）。图片不抽文本。允许后缀：`.jpg .jpeg .png .webp .txt .md .doc .docx`（大小写不敏感）。

- [ ] **Step 1: Write the failing test**

```python
import pytest

from app.services.attachments import UploadRejected, classify, extract_text, save_bytes


def test_classify_and_reject():
    assert classify("a.PNG") == "image"
    assert classify("b.md") == "document"
    with pytest.raises(UploadRejected) as ei:
        classify("x.exe")
    assert ei.value.status_code == 400


def test_save_rejects_oversize(tmp_path):
    with pytest.raises(UploadRejected) as ei:
        save_bytes(str(tmp_path), 1, "a.txt", b"hello", max_bytes=3)
    assert ei.value.status_code == 400


def test_extract_txt(tmp_path):
    p = tmp_path / "a.txt"
    p.write_text("hello 社会学", encoding="utf-8")
    assert extract_text(str(p), ".txt") == "hello 社会学"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && PYTHONPATH=. pytest tests/test_attachments.py -v`

Expected: FAIL missing module

- [ ] **Step 3: Write minimal implementation**

`backend/app/services/attachments.py`:

```python
from __future__ import annotations

import subprocess
from pathlib import Path

from docx import Document

IMAGE_EXT = {".jpg", ".jpeg", ".png", ".webp"}
DOC_EXT = {".txt", ".md", ".doc", ".docx"}


class UploadRejected(Exception):
    def __init__(self, status_code: int, detail: str):
        self.status_code = status_code
        self.detail = detail
        super().__init__(detail)


def classify(filename: str) -> str:
    suffix = Path(filename).suffix.lower()
    if suffix in IMAGE_EXT:
        return "image"
    if suffix in DOC_EXT:
        return "document"
    raise UploadRejected(400, "不支持的文件类型")


def save_bytes(
    upload_dir: str, message_id: int, filename: str, data: bytes, max_bytes: int
) -> str:
    if len(data) > max_bytes:
        raise UploadRejected(400, "文件过大")
    safe = Path(filename).name
    dest_dir = Path(upload_dir) / str(message_id)
    dest_dir.mkdir(parents=True, exist_ok=True)
    dest = dest_dir / safe
    dest.write_bytes(data)
    return str(dest.resolve())


def extract_text(path: str, suffix: str) -> str | None:
    suffix = suffix.lower()
    if suffix in {".txt", ".md"}:
        return Path(path).read_text(encoding="utf-8", errors="replace")
    if suffix == ".docx":
        return "\n".join(p.text for p in Document(path).paragraphs)
    if suffix == ".doc":
        try:
            proc = subprocess.run(
                ["antiword", path], capture_output=True, timeout=30, check=False
            )
        except FileNotFoundError:
            return None
        if proc.returncode != 0:
            return None
        return proc.stdout.decode("utf-8", errors="replace")
    return None
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && PYTHONPATH=. pytest tests/test_attachments.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/attachments.py backend/app/services/__init__.py backend/tests/test_attachments.py
git commit -m "feat: add attachment validation, storage, and text extraction"
```

---

### Task 8: 上下文组装与截断

**Files:**
- Create: `backend/app/services/context.py`
- Create: `backend/tests/test_context.py`

**Interfaces:**
- Consumes: 内存结构 `list[dict]`，每项 `{"role","content","attachments":[{"kind","storage_path","extracted_text"}]}`。不读取 `reasoning` 字段。
- Produces: `build_openai_messages(items: list[dict]) -> list[dict]`（OpenAI chat 格式；文档把 `extracted_text` 追加进 user 文本；图片用 `{"type":"image_url","image_url":{"url": "data:image/png;base64,..."}}` 的 content 数组）；`drop_oldest_turn(items: list[dict]) -> list[dict]` 丢掉最早一条 user 及其后紧跟的一条 assistant（若有）；`is_context_length_error(status_code: int, body_text: str) -> bool` 在 400/413 且正文含 `context_length` / `too many tokens` / `maximum context` 时为 True。截断时不得丢掉 `items[-1]`（当前轮，含附件）。

- [ ] **Step 1: Write the failing test**

```python
from app.services.context import build_openai_messages, drop_oldest_turn, is_context_length_error


def test_omits_reasoning_and_adds_doc_text():
    msgs = build_openai_messages(
        [
            {
                "role": "user",
                "content": "看这个",
                "attachments": [
                    {"kind": "document", "storage_path": "", "extracted_text": "人类学笔记"}
                ],
            },
            {"role": "assistant", "content": "好", "attachments": []},
        ]
    )
    assert msgs[0]["role"] == "user"
    assert "人类学笔记" in msgs[0]["content"]
    assert "reasoning" not in msgs[1]


def test_drop_oldest_keeps_last():
    items = [
        {"role": "user", "content": "a", "attachments": []},
        {"role": "assistant", "content": "b", "attachments": []},
        {"role": "user", "content": "c", "attachments": [{"kind": "image"}]},
    ]
    out = drop_oldest_turn(items)
    assert out[-1]["content"] == "c"
    assert out[-1]["attachments"][0]["kind"] == "image"


def test_context_length_detect():
    assert is_context_length_error(400, '{"error":{"message":"context_length_exceeded"}}')
    assert not is_context_length_error(500, "oops")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && PYTHONPATH=. pytest tests/test_context.py -v`

Expected: FAIL missing module

- [ ] **Step 3: Write minimal implementation**

`backend/app/services/context.py`:

```python
from __future__ import annotations

import base64
from pathlib import Path


def is_context_length_error(status_code: int, body_text: str) -> bool:
    if status_code not in {400, 413}:
        return False
    lowered = body_text.lower()
    return (
        "context_length" in lowered
        or "too many tokens" in lowered
        or "maximum context" in lowered
    )


def drop_oldest_turn(items: list[dict]) -> list[dict]:
    if len(items) <= 1:
        return items
    rest = items[1:]
    if rest and rest[0]["role"] == "assistant" and len(rest) > 1:
        rest = rest[1:]
    return rest


def _user_text(item: dict) -> str:
    parts = [item.get("content") or ""]
    for att in item.get("attachments") or []:
        if att.get("kind") == "document" and att.get("extracted_text"):
            parts.append(att["extracted_text"])
    return "\n".join(p for p in parts if p)


def _image_data_url(path: str) -> str:
    raw = Path(path).read_bytes()
    b64 = base64.b64encode(raw).decode("ascii")
    suffix = Path(path).suffix.lower().lstrip(".")
    mime = {"jpg": "jpeg", "jpeg": "jpeg", "png": "png", "webp": "webp"}.get(suffix, "png")
    return f"data:image/{mime};base64,{b64}"


def build_openai_messages(items: list[dict]) -> list[dict]:
    out: list[dict] = []
    for item in items:
        if item["role"] == "assistant":
            out.append({"role": "assistant", "content": item.get("content") or ""})
            continue
        text = _user_text(item)
        images = [
            a
            for a in (item.get("attachments") or [])
            if a.get("kind") == "image" and a.get("storage_path")
        ]
        if not images:
            out.append({"role": "user", "content": text})
            continue
        content: list[dict] = [{"type": "text", "text": text or " "}]
        for img in images:
            content.append(
                {"type": "image_url", "image_url": {"url": _image_data_url(img["storage_path"])}}
            )
        out.append({"role": "user", "content": content})
    return out
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && PYTHONPATH=. pytest tests/test_context.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/context.py backend/tests/test_context.py
git commit -m "feat: assemble OpenAI chat context without reasoning"
```

---

### Task 9: 上游流式客户端

**Files:**
- Create: `backend/app/services/llm.py`
- Create: `backend/tests/test_llm.py`

**Interfaces:**
- Consumes: httpx。
- Produces: `class StreamEvent`：`kind: str`（`"delta"|"reasoning"|"done"|"error"`），`text: str = ""`，`status_code: int | None = None`；`async def stream_chat_completion(*, base_url: str, api_key: str, model: str, messages: list[dict], timeout: float = 120.0, client: httpx.AsyncClient | None = None)` yield `StreamEvent`。请求 `POST {base_url}/chat/completions`，JSON `model, messages, stream=true`，header `Authorization: Bearer {api_key}`。`delta.content` → delta；`delta.reasoning_content` 或 `delta.reasoning` → reasoning；`[DONE]` → done；HTTP 非 200 → error（`text` 为正文前 500 字，`status_code` 为状态码）。

- [ ] **Step 1: Write the failing test**

用 `httpx.MockTransport` 返回：

```
data: {"choices":[{"delta":{"reasoning_content":"think","content":"hi"}}]}

data: [DONE]
```

断言收到 reasoning `think`、delta `hi`、done。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && PYTHONPATH=. pytest tests/test_llm.py -v`

Expected: FAIL missing module

- [ ] **Step 3: Write minimal implementation**

在 `backend/requirements.txt` 追加 `pytest-asyncio==0.25.2`。

`backend/app/services/llm.py`:

```python
from __future__ import annotations

import json
from collections.abc import AsyncIterator
from dataclasses import dataclass

import httpx


@dataclass
class StreamEvent:
    kind: str
    text: str = ""
    status_code: int | None = None


def _join_url(base_url: str, path: str) -> str:
    return base_url.rstrip("/") + path


async def stream_chat_completion(
    *,
    base_url: str,
    api_key: str,
    model: str,
    messages: list[dict],
    timeout: float = 120.0,
    client: httpx.AsyncClient | None = None,
) -> AsyncIterator[StreamEvent]:
    own = client is None
    client = client or httpx.AsyncClient(timeout=timeout)
    url = _join_url(base_url, "/chat/completions")
    headers = {"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"}
    payload = {"model": model, "messages": messages, "stream": True}
    try:
        async with client.stream("POST", url, json=payload, headers=headers) as resp:
            if resp.status_code != 200:
                body = (await resp.aread())[:500].decode("utf-8", errors="replace")
                yield StreamEvent(kind="error", text=body, status_code=resp.status_code)
                return
            async for line in resp.aiter_lines():
                if not line.startswith("data:"):
                    continue
                data = line[5:].strip()
                if data == "[DONE]":
                    yield StreamEvent(kind="done")
                    return
                try:
                    parsed = json.loads(data)
                except json.JSONDecodeError:
                    continue
                delta = (parsed.get("choices") or [{}])[0].get("delta") or {}
                reasoning = delta.get("reasoning_content") or delta.get("reasoning")
                if reasoning:
                    yield StreamEvent(kind="reasoning", text=reasoning)
                content = delta.get("content")
                if content:
                    yield StreamEvent(kind="delta", text=content)
            yield StreamEvent(kind="done")
    finally:
        if own:
            await client.aclose()
```

`backend/tests/test_llm.py` 使用 `httpx.MockTransport` + `httpx.AsyncClient(transport=...)` 传入 `client=`。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && PYTHONPATH=. pytest tests/test_llm.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/llm.py backend/tests/test_llm.py
git commit -m "feat: stream OpenAI-compatible chat completions"
```

---

### Task 10: 发送消息（校验、入库、SSE）

**Files:**
- Create: `backend/app/routers/messages.py`
- Create: `backend/tests/test_messages_send.py`
- Modify: `backend/app/main.py`

**Interfaces:**
- Consumes: `LlmModel`、`ChatSession`、`Message`、`Attachment`、`classify`/`save_bytes`/`extract_text`、`build_openai_messages`/`drop_oldest_turn`/`is_context_length_error`、`stream_chat_completion`、`auto_title`。
- Produces: `POST /api/sessions/{id}/messages` multipart 字段 `content`（可空字符串）、`model_id`、文件字段名 `files`（多文件）。SSE `text/event-stream`：`event: delta|reasoning|truncated|done|error`，data 为 JSON `{"text":...}` 或 done 时 `{"message_id": int}`。校验失败 HTTP 4xx 且不写库：未选/不存在模型 400「请先在设置中添加模型」；content 与文件都空 400；有图片且 `supports_vision` 为假 400「请更换支持视觉的模型」。成功路径：先插 user 消息与附件（快照 `model_name=LlmModel.name`、`model=LlmModel.model`）→ 流式 → 完整 assistant 再入库（同快照，`reasoning` 可空）→ 更新 session `updated_at` → 若标题仍为「新对话」则 `auto_title`。上游失败：不写半截 assistant，SSE error。上下文超长：循环 `drop_oldest_turn` 直至成功或只剩当前轮，每次重试前发 `truncated` 事件，文案「上下文过长，已截断较早消息」。

- [ ] **Step 1: Write the failing test**

1. 未登录 401。
2. 登录后无模型发送 400。
3. 空 content 无文件 400。
4. 建模型后 mock `stream_chat_completion` 为异步生成器 yield delta `"hello"` 再 done：POST 后 DB 有 user+assistant，`model_name` 为展示名，assistant.content == `"hello"`。

使用 `app.dependency_overrides` 或 `monkeypatch.setattr("app.routers.messages.stream_chat_completion", fake)`。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && PYTHONPATH=. pytest tests/test_messages_send.py -v`

Expected: FAIL 404

- [ ] **Step 3: Write minimal implementation**

`backend/app/routers/messages.py` 全文见本任务下方代码块：multipart 读 `content`/`model_id`/`files`；校验失败不写库；成功先落 user 再 `StreamingResponse`。`_to_item` 不含 `reasoning`。上游 `StreamEvent.kind == "error"` 且 `is_context_length_error` 时 `drop_oldest_turn` 并发送 `truncated`。`monkeypatch.setattr("app.routers.messages.stream_chat_completion", fake)`。

```python
from __future__ import annotations

import json
from collections.abc import AsyncIterator
from datetime import datetime, timezone
from pathlib import Path

from fastapi import APIRouter, Depends, HTTPException, Request
from fastapi.responses import StreamingResponse
from sqlalchemy.orm import Session, selectinload

from app.db import get_db
from app.deps import get_current_user
from app.services.attachments import UploadRejected, classify, extract_text, save_bytes
from app.services.context import build_openai_messages, drop_oldest_turn, is_context_length_error
from app.services.llm import stream_chat_completion
from app.tables import Attachment, ChatSession, LlmModel, Message, User
from app.title import auto_title

router = APIRouter(prefix="/api/sessions", tags=["messages"])


def _sse(event: str, payload: dict) -> str:
    return f"event: {event}\ndata: {json.dumps(payload, ensure_ascii=False)}\n\n"


def _to_item(msg: Message) -> dict:
    return {
        "role": msg.role,
        "content": msg.content or "",
        "attachments": [
            {
                "kind": a.kind,
                "storage_path": a.storage_path,
                "extracted_text": a.extracted_text,
            }
            for a in msg.attachments
        ],
    }


async def _generate(db, session, model_row, user_msg) -> AsyncIterator[str]:
    rows = (
        db.query(Message)
        .options(selectinload(Message.attachments))
        .filter(Message.session_id == session.id)
        .order_by(Message.id.asc())
        .all()
    )
    items = [_to_item(m) for m in rows]
    while True:
        openai_msgs = build_openai_messages(items)
        failed, failed_status = None, 0
        content_acc, reasoning_acc = "", ""
        async for ev in stream_chat_completion(
            base_url=model_row.base_url,
            api_key=model_row.api_key,
            model=model_row.model,
            messages=openai_msgs,
        ):
            if ev.kind == "error":
                failed, failed_status = ev.text, ev.status_code or 500
                break
            if ev.kind == "delta":
                content_acc += ev.text
                yield _sse("delta", {"text": ev.text})
            elif ev.kind == "reasoning":
                reasoning_acc += ev.text
                yield _sse("reasoning", {"text": ev.text})
            elif ev.kind == "done":
                break
        if failed is not None:
            if is_context_length_error(failed_status, failed) and len(items) > 1:
                items = drop_oldest_turn(items)
                yield _sse("truncated", {"text": "上下文过长，已截断较早消息"})
                continue
            yield _sse("error", {"text": failed})
            return
        assistant = Message(
            session_id=session.id,
            role="assistant",
            content=content_acc,
            reasoning=reasoning_acc or None,
            model_name=model_row.name,
            model=model_row.model,
        )
        db.add(assistant)
        session.updated_at = datetime.now(timezone.utc).replace(tzinfo=None)
        if session.title == "新对话":
            first_name = user_msg.attachments[0].original_filename if user_msg.attachments else None
            session.title = auto_title(user_msg.content, first_name)
        db.commit()
        db.refresh(assistant)
        yield _sse("done", {"message_id": assistant.id})
        return


@router.post("/{session_id}/messages")
async def send_message(
    session_id: int,
    request: Request,
    db: Session = Depends(get_db),
    user: User = Depends(get_current_user),
):
    form = await request.form()
    content = str(form.get("content") or "")
    model_id_raw = form.get("model_id")
    uploads = [v for k, v in form.multi_items() if k == "files" and hasattr(v, "filename")]
    settings = request.app.state.settings
    session = db.get(ChatSession, session_id)
    if session is None:
        raise HTTPException(status_code=404, detail="会话不存在")
    if not model_id_raw:
        raise HTTPException(status_code=400, detail="请先在设置中添加模型")
    try:
        model_id = int(model_id_raw)
    except (TypeError, ValueError):
        raise HTTPException(status_code=400, detail="请先在设置中添加模型")
    model_row = db.get(LlmModel, model_id)
    if model_row is None:
        raise HTTPException(status_code=400, detail="请先在设置中添加模型")
    if not content.strip() and not uploads:
        raise HTTPException(status_code=400, detail="请输入内容或上传文件")
    buffers: list[tuple[str, bytes, str]] = []
    try:
        for f in uploads:
            raw = await f.read()
            classify(f.filename or "")
            buffers.append((f.filename or "file", raw, f.content_type or "application/octet-stream"))
    except UploadRejected as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc
    if any(classify(name) == "image" for name, _, _ in buffers) and not model_row.supports_vision:
        raise HTTPException(status_code=400, detail="请更换支持视觉的模型")
    user_msg = Message(
        session_id=session.id,
        role="user",
        content=content,
        reasoning=None,
        model_name=model_row.name,
        model=model_row.model,
    )
    db.add(user_msg)
    db.flush()
    for filename, raw, mime in buffers:
        try:
            path = save_bytes(
                settings.upload_dir, user_msg.id, filename, raw, settings.max_upload_bytes
            )
        except UploadRejected as exc:
            db.rollback()
            raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc
        kind = classify(filename)
        extracted = extract_text(path, Path(filename).suffix) if kind == "document" else None
        db.add(
            Attachment(
                message_id=user_msg.id,
                kind=kind,
                original_filename=filename,
                mime_type=mime,
                storage_path=path,
                extracted_text=extracted,
            )
        )
    db.commit()
    user_msg = (
        db.query(Message)
        .options(selectinload(Message.attachments))
        .filter(Message.id == user_msg.id)
        .one()
    )
    return StreamingResponse(
        _generate(db, session, model_row, user_msg),
        media_type="text/event-stream",
        headers={"Cache-Control": "no-cache", "X-Accel-Buffering": "no"},
    )
```

`create_app` include 该 router（与 sessions router 共用前缀 `/api/sessions`）。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && PYTHONPATH=. pytest tests/test_messages_send.py tests/test_context.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/routers/messages.py backend/app/main.py backend/tests/test_messages_send.py
git commit -m "feat: send chat messages with SSE, snapshots, and truncation"
```

---

### Task 11: 消息分页

**Files:**
- Modify: `backend/app/routers/messages.py`
- Create: `backend/tests/test_messages_list.py`

**Interfaces:**
- Consumes: `Message`、`Attachment`。
- Produces: `GET /api/sessions/{id}/messages?before_id=&limit=`。`limit` 默认 50，最大 100，最小 1。`before_id` 空：该 session 按 `id` 倒序取 limit 条。`before_id` 有值：`id < before_id` 再倒序 limit。响应 JSON `{"messages":[...]}`，数组按 `id` **升序**（服务端反转）。每条含 `id, role, content, reasoning, model_name, model, created_at, attachments:[{id, kind, original_filename, mime_type}]`。不返回 `storage_path`。缺 session 404。

- [ ] **Step 1: Write the failing test**

插入 3 条消息，`GET limit=2` 应返回 id 较大的两条且升序；再 `before_id=` 其中最小 id，应返回更早那一条。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && PYTHONPATH=. pytest tests/test_messages_list.py -v`

Expected: FAIL 404 or missing key

- [ ] **Step 3: Write minimal implementation**

在 `backend/app/routers/messages.py` 追加 GET：

```python
@router.get("/{session_id}/messages")
def list_messages(
    session_id: int,
    before_id: int | None = None,
    limit: int = 50,
    db: Session = Depends(get_db),
    user: User = Depends(get_current_user),
):
    session = db.get(ChatSession, session_id)
    if session is None:
        raise HTTPException(status_code=404, detail="会话不存在")
    limit = min(max(limit, 1), 100)
    q = (
        db.query(Message)
        .options(selectinload(Message.attachments))
        .filter(Message.session_id == session_id)
    )
    if before_id is not None:
        q = q.filter(Message.id < before_id)
    rows = q.order_by(Message.id.desc()).limit(limit).all()
    rows.reverse()
    return {
        "messages": [
            {
                "id": m.id,
                "role": m.role,
                "content": m.content,
                "reasoning": m.reasoning,
                "model_name": m.model_name,
                "model": m.model,
                "created_at": m.created_at,
                "attachments": [
                    {
                        "id": a.id,
                        "kind": a.kind,
                        "original_filename": a.original_filename,
                        "mime_type": a.mime_type,
                    }
                    for a in m.attachments
                ],
            }
            for m in rows
        ]
    }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && PYTHONPATH=. pytest tests/test_messages_list.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/routers/messages.py backend/tests/test_messages_list.py
git commit -m "feat: paginate session messages for infinite scroll"
```

---

### Task 12: 联合搜索

**Files:**
- Create: `backend/app/services/search.py`
- Create: `backend/app/routers/search.py`
- Create: `backend/tests/test_search.py`
- Modify: `backend/app/main.py`

**Interfaces:**
- Consumes: `ChatSession`、`Message.content`（不搜 `reasoning`、不搜 `extracted_text`）。
- Produces: `tokenize_query(q: str) -> list[str]` 按空白 split 去空；`like_escape(s: str) -> str` 转义 `\` `%` `_`；`search_sessions(db, q: str) -> list[dict]`。空 token 列表：所有 session 按 `updated_at` 倒序，`snippet=""`，`hit_message_id=None`。非空：每个 token 必须满足 `title LIKE` **或** 存在某条 `content LIKE`。若标题单独满足全部 token，则 `hit_message_id` 为 `None`，`snippet` 为标题截断 80 字；否则 `hit_message_id` 为按 id 升序第一条 content 命中任一 token 的消息 id，`snippet` 为该 content 截断 80 字。`GET /api/search?q=` 返回 `{ "sessions": [...] }`，项为 `id, title, updated_at, snippet, hit_message_id`。

- [ ] **Step 1: Write the failing test**

建两个 session：标题「社会学笔记」内容「韦伯」；标题「闲聊」内容「韦伯 人类学」。查询 `韦伯 人类学` 只命中第二个。查询 `社会学` 命中第一个且 `hit_message_id is None`。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && PYTHONPATH=. pytest tests/test_search.py -v`

Expected: FAIL missing module

- [ ] **Step 3: Write minimal implementation**

`backend/app/services/search.py`:

```python
from sqlalchemy import or_
from sqlalchemy.orm import Session, selectinload

from app.tables import ChatSession, Message


def tokenize_query(q: str) -> list[str]:
    return [t for t in (q or "").split() if t]


def like_escape(s: str) -> str:
    return s.replace("\\", "\\\\").replace("%", "\\%").replace("_", "\\_")


def _title_has_all(title: str, tokens: list[str]) -> bool:
    return all(tok in title for tok in tokens)


def search_sessions(db: Session, q: str) -> list[dict]:
    tokens = tokenize_query(q)
    query = (
        db.query(ChatSession)
        .options(selectinload(ChatSession.messages))
        .order_by(ChatSession.updated_at.desc())
    )
    if tokens:
        for tok in tokens:
            pattern = f"%{like_escape(tok)}%"
            query = query.filter(
                or_(
                    ChatSession.title.like(pattern, escape="\\"),
                    ChatSession.messages.any(Message.content.like(pattern, escape="\\")),
                )
            )
    rows = query.all()
    out: list[dict] = []
    for row in rows:
        if not tokens:
            out.append(
                {
                    "id": row.id,
                    "title": row.title,
                    "updated_at": row.updated_at,
                    "snippet": "",
                    "hit_message_id": None,
                }
            )
            continue
        if _title_has_all(row.title, tokens):
            out.append(
                {
                    "id": row.id,
                    "title": row.title,
                    "updated_at": row.updated_at,
                    "snippet": row.title[:80],
                    "hit_message_id": None,
                }
            )
            continue
        hit = None
        for msg in sorted(row.messages, key=lambda m: m.id):
            if any(tok in (msg.content or "") for tok in tokens):
                hit = msg
                break
        out.append(
            {
                "id": row.id,
                "title": row.title,
                "updated_at": row.updated_at,
                "snippet": (hit.content[:80] if hit else row.title[:80]),
                "hit_message_id": hit.id if hit else None,
            }
        )
    return out
```

`backend/app/routers/search.py`:

```python
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session

from app.db import get_db
from app.deps import get_current_user
from app.services.search import search_sessions
from app.tables import User

router = APIRouter(prefix="/api/search", tags=["search"])


@router.get("")
def search(q: str = "", db: Session = Depends(get_db), user: User = Depends(get_current_user)):
    return {"sessions": search_sessions(db, q)}
```

`create_app` include 该 router。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && PYTHONPATH=. pytest tests/test_search.py -v`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/search.py backend/app/routers/search.py backend/app/main.py backend/tests/test_search.py
git commit -m "feat: add AND keyword search across sessions"
```

---

### Task 13: 前端工程与登录页

**Files:**
- Create: `frontend/package.json`
- Create: `frontend/vite.config.js`
- Create: `frontend/index.html`
- Create: `frontend/src/main.js`
- Create: `frontend/src/api.js`
- Create: `frontend/src/App.vue`
- Create: `frontend/src/views/LoginView.vue`
- Create: `frontend/src/styles.css`

**Interfaces:**
- Consumes: `POST /api/auth/login`。
- Produces: Vite 开发代理 `/api` → `http://127.0.0.1:8000`；`login(username, password)` fetch `credentials: 'include'`；路由 `/login`；失败展示「用户名或密码错误」。

- [ ] **Step 1: Write the failing test**

Create `frontend/src/api.js` 中纯函数可测部分放到 `frontend/src/searchQuery.js`：

```javascript
export function tokenizeQuery(q) {
  return q.split(/\s+/).filter(Boolean)
}
```

Create `frontend/src/searchQuery.test.js`（vitest）：`tokenizeQuery('韦伯  人类学')` deep equal `['韦伯','人类学']`。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd frontend && npm init -y && npm install vue vue-router && npm install -D vite @vitejs/plugin-vue vitest && npx vitest run src/searchQuery.test.js`

Expected: FAIL 文件不存在

- [ ] **Step 3: Write minimal implementation**

`frontend/vite.config.js`:

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  test: { environment: 'node' },
  server: { proxy: { '/api': 'http://127.0.0.1:8000' } },
})
```

`package.json` scripts: `"dev": "vite"`, `"build": "vite build"`, `"test": "vitest run"`。

`frontend/src/api.js`：`login`、`logout`、`jsonFetch`（401 则 `window.location = '/login'`，登录页除外）。

`LoginView.vue`：两个输入、提交调 `login`，成功 `router.push('/')`。

`main.js` 创建 router：`/login` → LoginView，`/` 与 `/settings` 先占位空组件。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd frontend && npm test`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add frontend
git commit -m "feat: add Vue login page and Vite API proxy"
```

---

### Task 14: 三栏壳、Session 列表、设置页、输入框

**Files:**
- Create: `frontend/src/components/AppShell.vue`
- Create: `frontend/src/components/Composer.vue`
- Create: `frontend/src/views/ChatView.vue`
- Create: `frontend/src/views/SettingsView.vue`
- Modify: `frontend/src/api.js`
- Modify: `frontend/src/main.js`
- Modify: `frontend/src/styles.css`

**Interfaces:**
- Consumes: `/api/sessions`、`/api/models`、`/api/auth/password`。
- Produces: 左栏：搜索输入、「新对话」、session 列表、模型 `<select>`、「设置」链接；左栏底 `Composer`（textarea、file input `accept=".jpg,.jpeg,.png,.webp,.txt,.md,.doc,.docx"`、发送）；右栏占位「选择或新建对话」。`/settings` 仍包在 AppShell 内：模型表单字段 `name, base_url, api_key, model, supports_vision, sort_order`；改密表单 `old_password, new_password`。无模型时 select 为空，Composer 发送 alert「请先在设置中添加模型」。

- [ ] **Step 1: Write the failing test**

在 `frontend/src/api.js` 导出 `acceptAttr` 常量，vitest 断言它包含 `.png` 与 `.md`。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd frontend && npm test`

Expected: FAIL `acceptAttr` undefined

- [ ] **Step 3: Write minimal implementation**

CSS：`.shell { display:flex; height:100vh; }` `.left { width:280px; display:flex; flex-direction:column; }` `.left .nav { flex:1; overflow:auto; }` `.composer { margin-top:auto; }` `.right { flex:1; overflow:auto; }`。

`api.js` 增加 `listSessions`、`createSession`、`patchSession`、`deleteSession`、`listModels`、`createModel`、`updateModel`、`deleteModel`、`changePassword`。

ChatView 点列表项设置 `currentId`（Task 15 再拉消息）。新对话调用 `createSession` 并选中。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd frontend && npm test && npm run build`

Expected: PASS and build succeeds

- [ ] **Step 5: Commit**

```bash
git add frontend
git commit -m "feat: add three-pane shell, session list, settings, and composer"
```

---

### Task 15: 流式对话、无限滚动、搜索跳转

**Files:**
- Modify: `frontend/src/components/MessagePane.vue`（新建）
- Modify: `frontend/src/components/Composer.vue`
- Modify: `frontend/src/views/ChatView.vue`
- Modify: `frontend/src/api.js`

**Interfaces:**
- Consumes: `GET /api/sessions/{id}/messages`、`POST` SSE、`GET /api/search?q=`。
- Produces: `sendMessage({sessionId, content, modelId, files, onDelta, onReasoning, onTruncated, onDone, onError})` 用 `fetch` + `ReadableStream` 解析 `event:`/`data:`。MessagePane：正序渲染；助手 reasoning 默认折叠，按钮「查看推理」展开；顶部滚动接近 0 时用当前最小 `id` 作为 `before_id` 再请求，**保持滚动位置**（记录旧 `scrollHeight`）。搜索框 debounce 300ms 调 search；结果仍渲染在左栏列表；点击带 `hit_message_id` 时打开 session 并 `document.getElementById('msg-'+id).scrollIntoView()`。消息节点 id 为 `msg-{id}`。流式中在右栏追加临时 assistant 气泡。截断时 alert 或顶部条显示「上下文过长，已截断较早消息」。

- [ ] **Step 1: Write the failing test**

`frontend/src/sse.js`:

```javascript
export function parseSseChunk(buffer) {
  const parts = buffer.split('\n\n')
  const rest = parts.pop()
  const events = []
  for (const block of parts) {
    let event = 'message'
    let data = ''
    for (const line of block.split('\n')) {
      if (line.startsWith('event:')) event = line.slice(6).trim()
      if (line.startsWith('data:')) data += line.slice(5).trim()
    }
    if (data) events.push({ event, data: JSON.parse(data) })
  }
  return { events, rest }
}
```

测试：输入 `'event: delta\ndata: {"text":"hi"}\n\n'` 得到 `{event:'delta', data:{text:'hi'}}`。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd frontend && npm test`

Expected: FAIL missing `sse.js`

- [ ] **Step 3: Write minimal implementation**

实现 `sse.js`、`api.sendMessage` 循环读 stream。MessagePane 与 ChatView 接线。发送时 FormData：`content`、`model_id`、每个文件 `files`。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd frontend && npm test && npm run build`

Expected: PASS

本地手测（执行者本机）：终端 1 `cd backend && APP_USERNAME=admin APP_PASSWORD=passpass SESSION_SECRET=devsecret-devsecret-dev DATABASE_URL=sqlite:///./dev.db UPLOAD_DIR=./uploads PYTHONPATH=. uvicorn app.main:create_app --factory --reload`；终端 2 `cd frontend && npm run dev`。用浏览器走完 `FOR_LLM.md` 第 10 节第 1–6 条能测的部分（无真实模型 key 时至少验证 400 提示）。

- [ ] **Step 5: Commit**

```bash
git add frontend
git commit -m "feat: stream replies, infinite scroll, and search navigation"
```

---

### Task 16: Supervisor、Nginx、环境样例

**Files:**
- Create: `.env.example`
- Create: `deploy/chat-api.conf`
- Create: `deploy/nginx-chat.conf`
- Create: `deploy/run.sh`

**Interfaces:**
- Consumes: 构建产物 `frontend/dist`、uvicorn `app.main:create_app --factory`。
- Produces: 可在 Ubuntu 上复制的 Supervisor 与 Nginx 配置。Nginx：`root` 指向 dist；`location /api { proxy_pass http://127.0.0.1:8000; proxy_buffering off; proxy_http_version 1.1; proxy_set_header Connection ''; client_max_body_size 25m; }`。SPA fallback：`try_files $uri $uri/ /index.html`。Supervisor：`command=` venv 中的 uvicorn，`--host 127.0.0.1 --port 8000`，`autostart=true`，`autorestart=true`，`stdout_logfile`/`stderr_logfile` 指向 `/var/log/iicode-chat/`。`run.sh` 仅用于本机：`export` 必填变量后启动 uvicorn（不替代 Supervisor）。

- [ ] **Step 1: Write the failing test**

无运行时测试。用 shell 断言配置关键字存在：

```bash
test -f deploy/chat-api.conf && grep -q 'autorestart=true' deploy/chat-api.conf
test -f deploy/nginx-chat.conf && grep -q 'proxy_buffering off' deploy/nginx-chat.conf
test -f .env.example && grep -q 'SESSION_SECRET' .env.example
```

先不创建文件，命令应非零退出。

- [ ] **Step 2: Run test to verify it fails**

Run: `test -f deploy/chat-api.conf`

Expected: FAIL (file missing)

- [ ] **Step 3: Write minimal implementation**

`.env.example`:

```
APP_USERNAME=admin
APP_PASSWORD=change-me
SESSION_SECRET=replace-with-a-long-random-string
MYSQL_HOST=127.0.0.1
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=chat_db
UPLOAD_DIR=/var/lib/iicode-chat/uploads
MAX_UPLOAD_BYTES=20971520
```

`deploy/chat-api.conf`（把 `/opt/iicode-chat` 写成实际部署路径占位，执行时改成仓库路径）：

```
[program:chat-api]
directory=/opt/iicode-chat/backend
command=/opt/iicode-chat/backend/.venv/bin/uvicorn app.main:create_app --factory --host 127.0.0.1 --port 8000
user=www-data
autostart=true
autorestart=true
stdout_logfile=/var/log/iicode-chat/api.out.log
stderr_logfile=/var/log/iicode-chat/api.err.log
environment=APP_USERNAME="admin",APP_PASSWORD="change-me",SESSION_SECRET="replace-me",PYTHONPATH="/opt/iicode-chat/backend"
```

`deploy/nginx-chat.conf`:

```
server {
    listen 80;
    server_name _;
    root /opt/iicode-chat/frontend/dist;
    client_max_body_size 25m;

    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Cookie $http_cookie;
        proxy_buffering off;
        proxy_set_header Connection '';
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

`deploy/run.sh`:

```bash
#!/bin/sh
set -e
cd "$(dirname "$0")/../backend"
. .venv/bin/activate
export PYTHONPATH=.
exec uvicorn app.main:create_app --factory --host 127.0.0.1 --port 8000
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```bash
grep -q 'autorestart=true' deploy/chat-api.conf
grep -q 'proxy_buffering off' deploy/nginx-chat.conf
grep -q 'SESSION_SECRET' .env.example
```

Expected: exit 0

部署机额外步骤（执行者在 Ubuntu 上做，不写进应用代码）：`apt install antiword nginx supervisor`；`python3 -m venv backend/.venv`；`pip install -r backend/requirements.txt`；`mkdir -p /var/lib/iicode-chat/uploads /var/log/iicode-chat`；把 Supervisor/Nginx 配置链到 `/etc`；`cd frontend && npm ci && npm run build`；`supervisorctl reread && supervisorctl update`。对照 `FOR_LLM.md` 第 10 节第 7 条：停掉再启动 `chat-api` 后页面仍能对话。

- [ ] **Step 5: Commit**

```bash
git add .env.example deploy
git commit -m "chore: add Supervisor, Nginx, and env templates"
```

---

## Self-review

**Spec coverage**

| 规格条目 | Task |
|---------|------|
| 单用户登录/改密/Cookie `token_version` | 3, 4 |
| 启动必填三项配置 | 1 |
| `chat_db` / 五张表 / 模型快照字段 | 2, 10 |
| 模型 CRUD + 掩码 + `supports_vision` | 5 |
| Session 列表/新对话/改标题/删除+删文件 | 6 |
| 自动标题 40 字 | 6, 10 |
| 附件类型/20MB/docx/antiword | 7, 10 |
| 全量上下文、不传 reasoning、截断提示 | 8, 10, 15 |
| SSE + reasoning 入库 | 9, 10 |
| 分页无限滚动 | 11, 15 |
| AND 搜索、`hit_message_id` | 12, 15 |
| 左栏输入、右栏聊天、`/settings` | 14, 15 |
| Supervisor + Nginx `proxy_buffering off` | 16 |
| 不做 RAG/多用户/Docker | 全局约束，无对应功能任务 |

**Placeholder scan:** 无 TBD/TODO；部署路径 `/opt/iicode-chat` 是明确占位，执行时改成真实 clone 路径。

**Type consistency:** `ChatSession` 表名 `sessions`；Cookie `chat_session`；消息 multipart 字段 `content`、`model_id`、`files`；SSE 事件名 `delta|reasoning|truncated|done|error`；搜索返回 `hit_message_id`。前后端均使用这些名字。
